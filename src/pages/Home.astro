<section id="home" class="introduction-page gradient-shadow">
	<div class="images-container">
		<img class="main-image" src="house.webp" alt="">
	</div>
    <aside class="aside-info left-border">
        <h1 class="title">
            A <span style="color: blue">Costa</span>rri<span style="color: red">can</span> 
			Experience
        </h1>
        <p>Dive into a welcoming and friendly <span class="green">cultural experience</span>, 
			where you'll be treated as a cherished <span class="green">member</span> of the 
			<span class="green">family</span>!
        </p>
	</aside>
</section>

<style>
	:root {
		--min-width: 370px;
		--min-height: 300px;
	}

    .introduction-page {
		position: relative;
		width: 100%;
		aspect-ratio: 16/8.1;
		min-width: var(--min-width);
		min-height: var(--min-height);
	}

	.gradient-shadow {
		background: linear-gradient(
			to right,
			rgba(0, 0, 0, 0.5) 10%,
			transparent 25%,
			#09050b 70%,
			#161512 100%
		);
	}

	.images-container {
		position: absolute;
		z-index: -1;
		overflow: hidden;
		/* 100% of parent container */
		height: 100%;
		width: 66%;
		min-width: var(--min-width);
		min-height: var(--min-height);
	}

	.main-image  {
		aspect-ratio: 965/759;
		filter:saturate(130%) contrast(110%);
		object-fit: cover;
		width: 100%;
	}

	.aside-info {
		position: absolute;
		z-index: 0;
		top: 45%;
		right: 8%;

		.title {
			max-width: 450px;
			font-size: 4em;
			text-wrap: balance;
			font-family: 'Elsie Swash Caps', 'Jost Variable', system-ui;
			font-weight: 600;
		}

		& p {
			text-wrap: pretty;
			max-width: 400px;
			font-size: 1.2em;
		}
	}

	.left-border {
		padding: 20px;
		border-left: 6px solid var(--text-color);
		border-radius: 10px;
		display: flex;
		flex-direction: column;
		gap: 20px;
	}

	.green {
		color: rgb(255, 245, 47);
	}

	@media screen and (max-width: 1200px) {
		.aside-info {
			
			right: -3%;

			.title {
				font-size: 2.5em;
				max-width: 400px;
			}

			& p {
				font-size: 1.1em;
				max-width: 350px;
			}
		}
	}

	@media screen and (max-width: 820px) {

		.introduction-page {
			width: 100vw;
			height: 10px;
			display: flex;
			flex-direction: column;
			height: auto;
			aspect-ratio: 965/759;
		}

		.gradient-shadow {
			background: linear-gradient(
				to bottom,
				rgba(0, 0, 0, 0.6) 10%,
				transparent 40%,
				rgb(22, 21, 18, 0.8) 100%
			);
		}

		.images-container { width: 100vw; }

		.aside-info {
			top: 90%;
			left: 0;

			.title {
				max-width: 100vw;
			}

			& p {
				max-width: 430px;
			}
			
		}

		.left-border {
			padding: 12px;
			border-left: none;
		}
	}
	
</style>